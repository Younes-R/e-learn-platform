export interface Student {
  firstName: string;
  lastName: string;
  birthDate: Date;
  email: string;
  phoneNumber: string;
  profilePic: string;
  pwd: string;
  refreshToken: string;
  bio: string;
}

export interface Teacher {
  firstName: string;
  lastName: string;
  birthDate: Date;
  email: string;
  phoneNumber: string;
  profilePic: string;
  pwd: string;
  refreshToken: string;
  bio: string;
  address: string;
  cv: string;
  diploma: string;
}

export interface Moderator {
  firstName: string;
  lastName: string;
  birthDate: Date;
  email: string;
  phoneNumber: string;
  profilePic: string;
  pwd: string;
  refreshToken: string;
}
